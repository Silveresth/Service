[phases.setup]
nixPkgs = ["python311", "nodejs_20", "nodePackages.npm"]

[phases.build]
cmds = [
  "npm install --prefix frontend",
  "npm run build --prefix frontend",
  "pip install -r requirements.txt"
]

[start]
cmd = "python manage.py migrate && gunicorn monprojet.wsgi:application"