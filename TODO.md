# TODO

## Backend UI (root “backend started” page)
- [x] Identify endpoint: `service_market/service_market/urls.py` `api_root` returns JsonResponse.
- [ ] Replace `api_root` with a styled HTML page (title, status badge, links).
- [ ] If possible, add link to `/api/` and `/admin/`.

## Session deconnexion after time
- [ ] Check frontend refresh logic on 401 and display a clearer error when refresh fails.
- [ ] Optionally log refresh error details in `frontend/src/api/axios.js`.

